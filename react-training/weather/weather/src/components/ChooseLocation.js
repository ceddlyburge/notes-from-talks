// declare the Ingredient module
const ChooseLocation = ({location, locationChange}) =>
    <form>
        <input value={location} onChange={locationChange}/>
    </form>

// export default Ingredient
export default ChooseLocation