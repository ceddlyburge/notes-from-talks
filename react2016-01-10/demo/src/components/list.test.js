import React from 'react';
import { shallow } from 'enzyme';
import List from 'list';

it('should blah', () => {
    const actual = "gh";
    const expected = "kjh";

    const list = shallow(<List />);

    expect()
    }
);